function setAnswer1(){
  const element = document.getElementById('answer1');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>Meta티켓에서 이름변경을 진행하기 전에 반드시 본인명의 휴대폰통신사에 개명신청이 되어 있어야 합니다.<br>\
                        Meta티켓 고객센터 문의전화 02-134-5123(유료)로 문의 바랍니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer2(){
  const element = document.getElementById('answer2');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>ID/PW찾기를 통해 본인임이 확인되면 즉시 변경할 수 있습니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer3(){
  const element = document.getElementById('answer3');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>마이페이지에서 회원정보 수정 탭을 통해 회원탈퇴 가능합니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer4(){
  const element = document.getElementById('answer4');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>티켓응모는 Meta티켓서비스에서 응모 가능합니다. </td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer5(){
  const element = document.getElementById('answer5');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>마이페이지에 응모내역 탭을 선택 후 확인 가능 합니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer6(){
  const element = document.getElementById('answer6');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>티켓 응모는 1인 1매만 가능합니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer7(){
  const element = document.getElementById('answer7');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>결제 가능한 수단은 카카오페이 결제만 가능합니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer8(){
  const element = document.getElementById('answer8');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>카카오페이는 카카오톡 이용자면 누구나 사용할 수 있는 간편 결제이며 Meta티켓에서는 카드결제를 선택하여 사용할 수 있습니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer9(){
  const element = document.getElementById('answer9');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>당첨된 티켓은 양도가 불가능 합니다.</td>";
  }else{
    element.style.display = 'none';
  }
}

function setAnswer10(){
  const element = document.getElementById('answer10');
  if(element.style.display == 'none'){
    element.style.display = 'table-row';
    element.innerHTML = "<td colspan = '3';>Meta티켓 고객센터 문의전화 02-134-5123(유료)로 문의 바랍니다. </td>";
  }else{
    element.style.display = 'none';
  }
}
